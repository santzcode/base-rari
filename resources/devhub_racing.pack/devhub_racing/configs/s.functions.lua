OpenServerFunctions = {
    CanAddUserToRace = function(source, raceId, mapId)
        return true
    end,
    AddUserToRace = function(source, raceId, mapId)
        return
    end,
    RaceFinishByUser = function(source, raceId, position, mmrChange, moneyReward, raceTime)
        return
    end,
    RaceFinish = function(raceId, playersFinishOrder)
        return
    end,
    RaceLeave = function(source, raceId)
        return
    end,
    StartRace = function(raceId, mapId, startTime, laps, collisions, fpv, entryFee, vehicleClass, official, winnerReward)
        return
    end,
    RaceDelete = function(raceId)
        return
    end,
    CreateMap = function(mapId, name, checkpoints, decorations, raceType, isVerified, rating, start, finish, creator, creatorIdentifier)
        return
    end,
}